# 倍特工作室项目工作规范 / PROJECT WORKING RULES

更新时间：2026-09-14

> 本文件是仓库根目录的统一工作入口。处理“倍特工作室”相关 SQL、数据分析、埋点、看板、项目配置、口径核查、报错修复、跨项目迁移等需求时，除当前用户明确指令外，优先读取本文件，再按本文索引读取对应项目和专题知识。

## 1. 读取优先级

处理任何需求，默认按以下顺序取知识：

1. 当前用户本轮明确要求。
2. `PROJECT_WORKING_RULES.md`（本文件）：统一工作习惯、核心概念、知识路径和分析方法。
3. `project_memory.yaml`：项目映射、表后缀、稳定跨项目规则、成熟样本规则。
4. `assistant_sql_response_rules.yaml`：SQL 输出、修改、调试、标签/分群 JOIN 等规则。
5. 对应项目 tracking/config：如 `xiafangle_tracking.yaml`、`bubu_tracking.yaml`、`wanyue_tracking.yaml`、`baodanfeshe_tracking.yaml`、`luobo4_tracking.yaml`。
6. `memory/`、`memory/projects/` 中对应项目/专题的最新确认口径。
7. `sql/` 中最新已验证的同类 SQL；有同类实现时优先复用，不重新独立造一套逻辑。
8. `sql_templates.yaml` 通用模板。
9. 上述资料均不足时才允许推断；关键字段、单位、事件名、关联键不允许猜。

冲突优先级：

`当前用户明确要求 > 本文件最新规则/最新项目专题规则 > 最新已验证 SQL > 稳定全局规则 > 通用模板 > 推断`

长期有效的新规则、字段修正、单位修正、渠道映射、分子分母、成熟规则、关联键等，用户确认后需要同步回 GitHub，避免新会话继续使用旧口径。

---

## 2. SQL 编写习惯

### 2.1 基础风格

- 方言统一使用 TrinoSQL。
- 默认不用 `WITH` / CTE。
- 默认不用 `USING`，JOIN 条件明确写 `ON`。
- 默认返回最终完整、可执行 SQL，不只返回 diff 或替换片段。
- 用户提供了现有 SQL 时，以该 SQL 或仓库最新同类 SQL 为基础做最小必要修改，不无理由重写整个结构。
- 相同语义指标应保持同一实现方式；用户说“这个也改”“和竞技场一样”“都改累计”等时，复用同一 cohort、成熟、分层、JOIN 和输出框架。
- 展示列优先中文别名。
- 同类 D1-D7 / D30 指标尽量放在一起；人数、金额、率等按类型组织，避免留存人数和留存率混排。
- 金额展示通常保留两位；计算前必须先确认项目原始单位。
- 比例/率默认输出 0~1，不自动乘 100；正式指标通常保留四位，除非需求另有说明。
- 分母使用 `nullif(分母, 0)` 防止除零；NULL 是否补 0 要结合业务含义显式处理。
- 不编造字段、事件名、表后缀、支付单位、标签关联键。

### 2.2 修改与调试

- 修改 SQL：只改用户要求的业务对象或指标，其他已确认口径继承最新版本。
- 报错修复：先读准确报错，确认字段/类型/表，再修最小根因；不要为了修类型报错顺便改变业务定义。
- 修复一个字段类型或 JOIN 后，要顺带检查邻近的类型转换和关联键，避免下一条明显错误继续保留。
- 数据全 0、NULL 异常、倍率离谱时，优先写核查 SQL，不直接进入业务结论。

### 2.3 性能习惯

- 大事件表尽量先压缩到目标粒度再 JOIN，例如“日期 × 玩家 × 渠道”“日期 × 玩家 × 玩法”。
- `pay_log`、`in_out_log` 等明细在不需要逐事件时优先先聚合。
- 第一次发生日期优先 `GROUP BY + MIN`；需要对应最早值时优先 `min_by(value, time)`。
- 避免在原始超大明细上先做窗口函数再互相 JOIN，防止数据膨胀。

---

## 3. 数数日期与成熟样本

### 3.1 动态日期参数

- `${PartDate:date}`、`${PartDate:date1}` 等数数参数视为当前作用域中的完整 WHERE 条件。
- 不要再拼别名或 `$part_date` 字段，例如不要写成 `e.${PartDate:date}` 或 `"$part_date" ${PartDate:date}`。
- cohort 日期筛选和后续事件扫描是两件事：筛选 8 月新增，不代表 D1-D30 事件表也只能扫 8 月。
- 生命周期分析的事件扫描必须自动覆盖完整观察窗口。

### 3.2 D1/DN 统一理解

- 默认 D1 = cohort 当天。
- D7 对应 cohort 当天到第 7 个自然日，即 D0~D6 的完整窗口。
- D30 同理要求完整 30 天观察窗口。
- 用户要求当日指标时，只统计第 N 天当天；用户要求累计指标时，从 cohort 起点累计到第 N 天。

### 3.3 成熟口径

生命周期、LTV、留存、付费、活跃、关卡、阵容、活动周期等指标，默认使用“自动延展 + 完整成熟样本”：

- 先确定 cohort 锚点：创角日、注册日、开服日、活动开始日等。
- DN 指标只纳入已经完整走完 N 天窗口的 cohort。
- D7 成熟条件通常为 `cohort日期 + 6天 <= 昨天`。
- 未成熟新增日期、区服、活动周期不进入对应成熟指标。
- 禁止用当前残缺累计值冒充成熟 DN 指标。

---

## 4. 高频业务概念

- **cohort / 新增**：先明确用户被圈入分析的锚点日期。
- **DAU**：当天有项目指定活跃事件的玩家数。
- **留存人数**：cohort 玩家在目标日再次满足活跃条件的人数。
- **留存率**：目标日留存人数 / cohort 人数。
- **付费人数**：指定口径范围内存在有效正向付费的玩家数；有效条件按项目/专题确认。
- **付费率**：付费人数 / 对应分母；必须先明确分母是新增、DAU、参与人数还是其他 cohort。
- **ARPU**：流水 / 总玩家人数。
- **ARPPU**：流水 / 付费人数。
- **LTV**：cohort 生命周期累计付费 / cohort 人数。
- **付费留存**：付费 cohort 在后续目标日再次付费，不等于活跃留存。
- **首日付费**：只统计注册/创角当天实际 `pay_log`，不能用历史累充或查询区间累充代替。
- **首日付费分层**：先求首日金额，再分 R 档。
- **累计付费分层**：按生命周期/历史累充金额分层；必须和首日分层区分。
- **ad_platform**：默认从用户表取；空值展示为“自然量”。
- **当前分群表**：`ta.user_result_cluster_xx`；若使用账号字符串分析对象，`cast("#account_id" as varchar) = "#varchar_id"`。
- **历史标签表**：`ta.history_tag_xx`；用户级历史标签如 VIP，使用源表 `#user_id -> history_tag.#long_id`，历史版本看 `$tag_date`。

---

## 5. 项目核心配置入口

| 项目 | 后缀 | 用户表 | 事件表 | 高频注意事项 |
|---|---:|---|---|---|
| 下方了 | 41 | `ta.v_user_41` | `ta.v_event_41` | `payment/100`；`create_role_time` 为 timestamp；活跃 `in_out_log`；主线开始 `battle_star`；主线 `battle_type=1` |
| 步步 / 一步两步 | 22 | `ta.v_user_22` | `ta.v_event_22` | 活跃 `log_in_out`；v21/v2 旧口径作废 |
| 弯月勇者 | 44 | `ta.v_user_44` | `ta.v_event_44` | 固定 v44；支付单位未验证时禁止自行 `/100`；战斗开始 `battle_star` |
| 暴弹飞射 | 42 | `ta.v_user_42` | `ta.v_event_42` | `payment` 为元；在线时长用 `ta_app_end.#duration`；战斗开始 `battle_start` |
| 萝卜4小游戏 | 37 | `ta.v_user_37` | `ta.v_event_37` | 用户表无 `domain/create_role_time`；新增取 `register.$part_date` |
| 喵喵岛 | 待确认 | 待确认 | 待确认 | 仍处埋点方案阶段，SQL 前必须先确认线上表 |

完整项目配置优先读 `project_memory.yaml` 和对应 `*_tracking.yaml`，不要只依赖本表摘要。

---

## 6. 《下方了》渠道特别规则

### 6.1 后台渠道配置与报表渠道池要分开

完整后台渠道配置以 `memory/projects/下方了项目表口径.md` 最新表为准。已确认包括：

| channel_id | 当前后台名称 |
|---|---|
| 3056210 | TapTap |
| 3056211 | 微信小游戏 |
| 3056214 | 抖音小游戏 |
| 3056215 | 官方Apple渠道 |
| 3056218 | 主播直播体验包 |
| 3056219 | 抖音直播投放包 |
| 3056220 | 支付宝小游戏 |
| 3056221 | 好游快爆 |
| 3056222 | 官方Android渠道 |

注意：某张报表使用的业务渠道池不等于完整后台渠道配置。旧 SQL 注释如果与后台配置冲突，展示渠道名称必须以最新后台配置为准。

### 6.2 跨渠道身份

- 下方了跨渠道唯一玩家默认使用 `nb_open_id`。
- `active_channel`：同一 `nb_open_id` 下最早 `create_role_time` 对应的 `channel_id`。
- `event_channel`：当天实际登录/支付事件上报的 `channel_id`。
- 渠道新增玩家：同一 `nb_open_id` 第一次进入该实际行为渠道的当天。

### 6.3 微信转端报表最新分类（2026-09-14）

当前指定微信转端报表的端包行为渠道池：`3056210 / 3056215 / 3056219`。

- `active_channel = 3056211`，后续进入 `3056210/3056215/3056219` → **转端用户**。
- `active_channel` 本身属于 `3056210/3056215/3056219`，行为渠道也属于该集合 → **端包用户**。
- `3056214` 抖音小游戏、`3056220` 支付宝小游戏不进入这套微信转端分类。
- 旧版“纯新/自有新增”属于历史跨渠道报表标签，不与这套最新转端分类混用。

该专题更详细规则优先读 `memory/projects/下方了项目表口径.md` 及相关渠道迁移专题文件。

---

## 7. GitHub 知识路径

### 7.1 根目录核心文件

- `PROJECT_WORKING_RULES.md`：**统一第一入口**，工作习惯、概念、项目索引、分析方法。
- `project_memory.yaml`：项目映射、稳定全局规则、成熟样本规则。
- `assistant_sql_response_rules.yaml`：SQL 调整、调试、输出规则。
- `sql_templates.yaml`：可复用 SQL 模板。
- `tracking_projects.yaml`：项目 → tracking 文件索引。
- `xiafangle_tracking.yaml`：下方了埋点。
- `bubu_tracking.yaml`：步步埋点。
- `wanyue_tracking.yaml`：弯月勇者埋点。
- `baodanfeshe_tracking.yaml`：暴弹飞射埋点。
- `luobo4_tracking.yaml`：萝卜4埋点。

### 7.2 项目/专题知识

- `memory/`：专题知识。
- `memory/projects/`：项目稳定规则、修正记录、专题口径。
- 典型：`memory/projects/下方了项目表口径.md`。
- 典型：`memory/projects/下方了渠道迁移收入分析口径_20260828.md`。

### 7.3 已验证实现

- `sql/`：已经验证或可复用的 SQL。
- 同类需求优先找到最新实现，在此基础上修改。

### 7.4 Skill

- `.codex/skills/game-data-sql-analyst/`：SQL 主 Skill。
- `.codex/skills/game-data-sql-analyst/SKILL.md`：SQL 工作流和知识读取顺序。
- `.codex/skills/game-data-sql-analyst/references/sql-review-checklist.md`：出 SQL 前检查清单。
- `.codex/skills/tracking-yaml/`：埋点表 → YAML 的 Skill。

### 7.5 操作和工作流

- `*_operation_map.yaml`：数数、引力、微信广告等系统定位和操作地图。
- `*_workflow.yaml`：固定分析/看板操作流程。

---

## 8. 分析习惯

### 8.1 分析顺序

默认分析路径：

`指标异常 → 核对口径 → 拆人群 → 拆生命周期/行为节点 → 定位玩法/付费项/资源 → 对照验证 → 输出结论`

不要看到总体涨跌就直接归因版本好坏。

### 8.2 常见拆分方式

1. **先看总体**：留存、LTV、付费率、ARPU、ARPPU、流水、关卡、战力等。
2. **再拆人群**：免费/小R/中R/大R、首日付费档、累计付费档、渠道、平台、区服、开服天数。
3. **再找节点**：具体 D2/D3/D7、关卡、系统开放日、礼包、活动、英雄、资源、玩法参与。
4. **再判断结构原因**：人数结构变化、行为转化变化、付费深度变化、内容承接不足等。
5. **最后做对照**：版本前后、测试前后、强弱服务器、活动前后、不同 cohort 对照。

### 8.3 结论表达

- 优先回答：**哪里变了、哪些人变了、在哪个节点变了、影响了什么**。
- 少写泛泛描述；通常收敛到 3~4 个核心观点。
- 异常值先核查，不直接解释。
- 结论需要能回到数据指标和具体行为节点。
- 周报/汇报按项目拆分；需要只写工作事项时，不展开具体分析结论。

---

## 9. 默认需求处理流程

以后处理需求默认执行：

1. 识别项目和任务类型。
2. **先读取 `PROJECT_WORKING_RULES.md`。**
3. 读取 `project_memory.yaml` 和对应项目/专题最新知识。
4. 搜索 `sql/` 是否有最新同类已验证实现。
5. 明确：cohort、日期、成熟、当日/累计、分子、分母、金额单位、用户身份和 JOIN 键。
6. 新建或最小化修改 SQL。
7. 执行 SQL review checklist。
8. 默认返回最终完整 SQL。
9. 用户确认了长期有效的新规则或纠正后，同步回 GitHub 对应知识文件；必要时同步更新本文件摘要。

---

## 10. 维护原则

- 本文件只保存跨需求高频、稳定、值得优先读取的工作规范和索引，不把所有专题细节复制进来。
- 项目详细业务规则仍放在 tracking、`memory/projects/`、专题文件和 `sql/` 中。
- 当项目配置、渠道、表后缀、金额单位、成熟规则、关联键等发生稳定变更时：
  1. 先更新对应项目知识源；
  2. 如果会影响多数需求，再同步更新本文件摘要；
  3. 旧规则需要明确标记历史版本，不能让新旧口径无说明并存。
